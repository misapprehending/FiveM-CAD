<?php
    $version = "1.0";
    //Database Settings
    $db_ip = "localhost";
    $db_username = "root";
    $db_password = "";
    $db_table = "fivem_cad";

    //Server Settings
    $server_name = "BushChook RP";
    $server_ip = "103.212.224.34";
    $server_port = ""; //If this is blank it will default to 30120
    $server_game = "Five"; // Five OR Red (This will implement features later, it does affect the "join server button")

    // CAD Settings
    $url = "localhost"; // cad.example.com
    $access_password = ""; // Set an access password to restrict access to the CAD
    $public_records = true; //True or False -- Enable the public to access criminal records

    //Logo Settings
    $server_logo = "http://$url/imgs/Logo.png"; // Server Logo URL (Set to null for none)
    $logo_scale = "10"/*%*/; // Scale is a percentage
?>